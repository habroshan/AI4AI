# Datasets (placeholders)
This folder is git‑ignored. We do not distribute any datasets.

Datasets used in the paper:
- MNIST (auto‑downloaded by torchvision)
- CIFAR‑10 (auto‑downloaded by torchvision)
- NIH ChestXray14 (must be obtained separately; see data/chestxray14/README.md)

Run `python scripts/check_data.py` to verify expected files/paths.
